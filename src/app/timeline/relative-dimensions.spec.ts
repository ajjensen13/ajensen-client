import { RelativeDimensions } from './relative-dimensions';

describe('RelativeDimensions', () => {
  it('should create an instance', () => {
    expect(new RelativeDimensions()).toBeTruthy();
  });
});
